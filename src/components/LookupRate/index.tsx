import React from 'react';
import {View, Text} from 'react-native';
import {styles} from './styles';

const LookupRate = () => {
  return (
    <View style={styles.container}>
      <Text>Lookup, exchange rate</Text>
    </View>
  );
};

export default LookupRate;
